# Frontend (Next.js 14 + Tailwind)

Quick start

1. Install dependencies

   cd frontend
   npm install

2. Run dev server

   npm run dev

## Notes

- The frontend expects the backend API to be available at the same origin (relative `/api` paths).
- For local development you can run the backend on port 4000 and configure a proxy, or run both and use CORS.
