module.exports = {
  plugins: {
    // For Tailwind v4 the PostCSS plugin package is `@tailwindcss/postcss`.
    // Use that plugin so that Tailwind's PostCSS integration works properly.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};

